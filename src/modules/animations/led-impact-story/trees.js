'use strict';

module.exports = {
	animIn,
};

function animIn(selectors, helpers){
	// const treeAsset = helpers.createElement('a-asset-item',{
	// 	'id': 'tree',
	// 	'src': 'assets/models/motion_green_tree_2/scene.gltf'
	// });
	// selectors.assets.appendChild(treeAsset);
	//
	// const tree = helpers.createElement('a-gltf-model',{
	// 	'id':'tree',
	// });
	// selectors.scene.appendChild(tree);
}
